# Worklog

Agent scratch log: state-changing work and decisions, newest first. Once a fact is settled it is PROMOTED to the proper doc (`decisions.md`, `SPEC.md`, `roadmap.md`, `docs/`) and DELETED from here. This file is not a permanent record.

Abbreviations: CI (continuous integration), CDN (content delivery network), OG (Open Graph), QR (quick-response code), SPAYD (Short Payment Descriptor &mdash; Czech QR payment standard).

**2026-07-07 (later) &mdash; seating plans embedded + gift unlinked + Gröbovka parking.** Praktické informace now carries the names-variant plans **inline** under Svatební hostina › Zasedací pořádek (injected by the generator between `seating-plans:begin/end` markers &mdash; fonts come from the page, defs ids suffixed, native-size-capped responsive via the global svg rule; standalone-open links beneath). The Dar section no longer links `gift.html` (direct `/gift` URL only; SPEC/architecture/CLAUDE.md synced). Doprava a parkování gains the vacate-after-ceremony note + "Parkování u Gröbovky (pro focení)" with two mapy.com links. Verified by desktop (1100px) + mobile (400px) renders; mobile relies on pinch-zoom / the standalone links for name-level reading &mdash; flagged for the real-device QA.

**2026-07-07 &mdash; reception seating schemes landed as tracked assets, in two variants.** Four SVG floor plans generated by `tools/generate-seating-schemes.py` (parametric, stdlib-only, single source of truth for all four &mdash; edit a coordinate or a name, re-run; never hand-edit the SVGs): `seating-{inside,outside}.svg` with numbered seats 1&ndash;45, and `seating-{inside,outside}-names.svg` with guest names on larger chips (80&times;46, long names wrapped to two lines; the terrace room widens in the names variant to fit the chips). Numbered variant reproduced from the owner's hand drawings through two annotated-screenshot review rounds; the refactor to two modes left the numbered SVGs byte-identical. NOT yet embedded in any page &mdash; that step needs a SPEC update + owner decisions (which page + which variant; inline `<svg>` vs `<img>` &mdash; `<img>` blocks the Google-Fonts import inside the SVG). Anything committed here is public once pushed (public repo + Pages deploy) &mdash; incl. the guest names in the names variant.

_Older work (the design refresh, the 4&rarr;3-page restructure, content, and map fixes) is settled and recorded in [decisions.md](./decisions.md), [SPEC.md](./SPEC.md), the dated plans in [archive-plans/](./archive-plans/), and the `docs/`._
