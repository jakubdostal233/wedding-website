# Worklog

Agent scratch log: state-changing work and decisions, newest first. Once a fact is settled it is PROMOTED to the proper doc (`decisions.md`, `SPEC.md`, `roadmap.md`, `docs/`) and DELETED from here. This file is not a permanent record.

Abbreviations: CI (continuous integration), CDN (content delivery network), OG (Open Graph), QR (quick-response code), SPAYD (Short Payment Descriptor &mdash; Czech QR payment standard).

**2026-07-07 &mdash; reception seating schemes landed as tracked assets.** Two SVG floor plans (`site/assets/img/seating-inside.svg` + `seating-outside.svg`, numbered seats 1&ndash;45, room landmarks) generated by the new `tools/generate-seating-schemes.py` (parametric, stdlib-only &mdash; edit a coordinate, re-run; never hand-edit the SVGs). Iterated from the owner's hand drawings + two annotated-screenshot review rounds. NOT yet embedded in any page &mdash; that step needs a SPEC update + owner decisions (which page; inline `<svg>` vs `<img>` &mdash; `<img>` blocks the Google-Fonts import inside the SVG; names-on-seats round implies guest names on the public site). Assets have public URLs after the next deploy even unlinked.

_Older work (the design refresh, the 4&rarr;3-page restructure, content, and map fixes) is settled and recorded in [decisions.md](./decisions.md), [SPEC.md](./SPEC.md), the dated plans in [archive-plans/](./archive-plans/), and the `docs/`._
