# Implementation plan

Living document &mdash; the current to-do list. Finished tasks are DELETED (not ticked); the file is reordered as priorities shift. Phase names are the canonical ones from [./roadmap.md](./roadmap.md); decision IDs (`D-`/`O-`) reference [./decisions.md](./decisions.md).

Current state: the site is **built and live** at <https://tereza-jakub.cz>. Done: the 2026-06-06 restructure ([D-STRUCT](./decisions.md#d-struct--repository-restructured-to-the-tyre-model-architecture)) + Actions deploy ([D-DEPLOY](./decisions.md#d-deploy--deploy-via-github-actions-serving-the-site-directory)), the blush-pink design refresh ([D-DESIGN](./decisions.md#d-design--visual-identity-refreshed-to-the-blush-pink-print-identity)), and the reduction to four navigated pages + an unlisted `gift.html` ([D-IA4](./decisions.md#d-ia4--site-reduced-to-four-pages)), with the real Czech content in place. Also done: the favicon refresh (blush-pink) and a 2026-07-03 QA pass &mdash; HTML validation, contrast, escaped font-link ampersands, Open Graph tags on `photoshooting.html`, a mobile fix for the gift-page bank-number wrap, and the finalized Svatební hostina + večerní-raut reception menus. What remains before the wedding (2026-07-10):

## Immediate (no blockers)

1. 🧪 **Owner-side real-device QA.** Run [../docs/qa-checklist.md](../docs/qa-checklist.md) on real phones/tablets (mobile data + Wi-Fi): the three maps, the `.ics` on iOS/Android, the `mailto:`, and the bank QR in **two** banking apps. The desktop + automated side is done (2026-07-03 QA pass); this is the physical-device part only you can do.
2. 🪑 **Embed the seating schemes** (`site/assets/img/seating-{inside,outside}.svg` numbered + `seating-{inside,outside}-names.svg` with guest names; all four generated by `tools/generate-seating-schemes.py`). Owner decisions first, then a SPEC update before touching pages: (a) which page/section (program? praktické informace? new unlisted page?) and which variant goes on it; (b) inline `<svg>` vs `<img>` &mdash; `<img>` blocks the SVG's Google-Fonts import, inline uses the page's fonts; (c) guest names are personal data &mdash; pushing the names variant makes them publicly reachable (public repo + Pages), owner said yes to building them, push confirmed separately.

## Mid-term (dependent)

- **Nicer hero photo** (you'll provide). Swap `site/assets/img/hero.{webp,jpg}`; keep &le; 250 KB, Czech `alt`. The current home-page photo is a casual candid &mdash; the highest-impact visual upgrade.

## Long-term (not essential now)

- **English `/en/` mirror** (the Phase 2 language option; may be skipped entirely).
- **Self-host fonts** ([O-FONTS](./decisions.md#o-fonts--self-host-fonts-versus-google-fonts-cdn)) if privacy becomes a concern &mdash; bundle Playfair Display + Source Sans 3 into `site/assets/` instead of the Google Fonts CDN.

## Open questions

These are the OPEN items from [./decisions.md](./decisions.md); resolve with the owner:

1. **[O-FONTS](./decisions.md#o-fonts--self-host-fonts-versus-google-fonts-cdn)** &mdash; self-host fonts versus the Google Fonts CDN.

## Abbreviations

CDN &mdash; content delivery network; CSS &mdash; Cascading Style Sheets; OG &mdash; Open Graph; QR &mdash; quick-response code; SPAYD &mdash; Short Payment Descriptor (Czech QR payment standard); WCAG AA &mdash; Web Content Accessibility Guidelines, level AA.
